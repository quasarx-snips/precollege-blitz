# 1. Stage the files you changed
git add Core-Technical-Projects/02-Level-2-Simulations/cpu_emulator/

# 2. Save the changes locally with a message
git commit -m "Update cpu emulator files"

# 3. Force push to overwrite the remote branch
git push origin main --force